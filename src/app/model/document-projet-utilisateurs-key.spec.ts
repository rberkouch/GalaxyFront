import { DocumentProjetUtilisateursKey } from './document-projet-utilisateurs-key';

describe('DocumentProjetUtilisateursKey', () => {
  it('should create an instance', () => {
    expect(new DocumentProjetUtilisateursKey()).toBeTruthy();
  });
});
