import { DocumentProjetUtilisateurs } from './document-projet-utilisateurs';

describe('DocumentProjetUtilisateurs', () => {
  it('should create an instance', () => {
    expect(new DocumentProjetUtilisateurs()).toBeTruthy();
  });
});
